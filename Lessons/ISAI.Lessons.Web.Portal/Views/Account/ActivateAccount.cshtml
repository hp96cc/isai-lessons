@model ISAI.Lessons.Web.Portal.Models.ForgotPasswordViewModel
@{
    Layout = "~/Views/Shared/_LayoutPreLogin.cshtml";
    ViewBag.Title = "Activate Account";
}

<h2>@ViewBag.Title.</h2>

@using (Html.BeginForm("ActivateAccount", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    <h4>Enter your email</h4>
  
    @Html.ValidationSummary("", new { @class = "text-danger" })
    <div class="form-group">
 
            @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
        
    </div>
    <div class="form-group">

            <input type="submit" class="btn btn-primary block full-width m-b" value="Activate Account" />
   
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
